package;

/**
 * Validates code in examples
 * 
 * @author Mark Knol
 */
class Validator
{
	public static function validateSource(source:String):Bool
	{
		var isValid = true;
		
		// magic happens here
		
		return isValid;
	}
}